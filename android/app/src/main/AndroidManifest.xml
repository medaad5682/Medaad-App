<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- ✅ [CRASH-FIX] RECEIVE_BOOT_COMPLETED removed: BootReceiver is disabled
         below (autoStartOnBoot is false), so this permission is unused. An
         unused dangerous-adjacent permission is also a Play Console review
         flag worth avoiding. -->

    <application
        android:label="مــــداد"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        
        android:allowAudioPlaybackCapture="false"

        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        
        android:allowBackup="false"
        android:fullBackupContent="false">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:replace="android:exported" />
            
        <!-- ✅ [CRASH-FIX] Disabled: main.dart sets autoStartOnBoot: false, so the
             app never intends to restart the download service on boot. This
             receiver was firing anyway on some OEMs/older cached installs and
             calling startForeground() from a BOOT_COMPLETED context, which
             Android 12+ blocks (ForegroundServiceStartNotAllowedException) and
             Android 14 additionally rejects for missing a runtime service type
             (MissingForegroundServiceTypeException — open upstream bug in
             flutter_background_service, see plugin issue #426). Disabling the
             receiver removes the trigger entirely instead of chasing the
             plugin's Android 14 type-declaration bug.
             If a future feature genuinely needs boot-time resume, re-enable
             this AND set autoStartOnBoot: true AND confirm the plugin version
             has fixed #426 first. -->
        <receiver
    android:name="id.flutter.flutter_background_service.BootReceiver"
    android:exported="true"
    android:enabled="false"
    tools:replace="android:enabled,android:exported">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
    </intent-filter>
</receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
